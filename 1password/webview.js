module.exports = Franz => {

  /*const getMessages() => {
    const has_notification = document.querySelector(".alertMightyMessage").length;

    Franz.setBadge(0, has_notification);
  }

  Franz.loop(getMessages);

  if (typeof Franz.onNotify === 'function') {
    Franz.onNotify(notification => {
      if (typeof notification.title !== 'string') {
        notification.title = ((notification.title.props || {}).content || [])[0] || 'MightyText';
      }

      return notification;
    });
  }*/

}
